{
  "name": "prototype-leker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/orders.js && node --check src/db.js && node --check src/admin.js && node --check public/customer.js && node --check public/admin.js",
    "db:verify:olshop": "npx wrangler d1 info program-olshop-db --json > /tmp/program-olshop-db.json",
    "db:migrations:apply": "npx wrangler d1 migrations apply DB --remote",
    "deploy": "npm run db:verify:olshop && npm run db:migrations:apply && npx wrangler deploy"
  }
}
